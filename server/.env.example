# Model Configuration
# Path to the model file (relative to models directory or absolute path)
# In Docker, this will be relative to /models volume mount
# Example: MODEL_PATH=/models/gemma-2-2b-it.q4_k_m.gguf
MODEL_PATH=/models/default.gguf

# API Keys Configuration
# Multiple keys can be specified as comma-separated values
# Example: OPENAI_API_KEYS=key1,key2,key3
# Copy this file to .env in the server directory and add your actual keys

OPENAI_API_KEYS=kook-openai-testkey
OLLAMA_API_KEYS=kook-ollama-testkey
