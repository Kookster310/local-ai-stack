# This directory stores sensitive credentials
# Files here are gitignored and should never be committed

# Expected files:
# - id_rsa                    SSH private key
# - id_rsa.pub                SSH public key (optional)
# - known_hosts               SSH known hosts
# - gmail-credentials.json    Google OAuth credentials
# - gmail-token.json          Gmail API token
# - arr-config.yaml           Radarr/Sonarr API keys
# - moltbook-config.yaml      Moltbook API credentials
# - blog-config.yaml          Blog repository config
